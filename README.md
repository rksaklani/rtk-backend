# rtk-backend
#RUN THE PROJECT 
  --- Install node module using the npm i ----
  --- Add the config.env---
  Inside the config.env file add
  the following  variable

   DB =                                          
   PORT=
   SECRET_kEY=
   EMAIL=
   PASSWORD=
   JWT_SECRET=
